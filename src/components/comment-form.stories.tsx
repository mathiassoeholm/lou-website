import React from "react";
import { CommentForm } from "./comment-form";

export default {
  title: "CommentForm",
  component: CommentForm,
};

export const Basic = () => {
  return <CommentForm />;
};
